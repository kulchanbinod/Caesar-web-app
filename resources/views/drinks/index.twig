{% extends "master.twig" %}
{% block main %}

	<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <h1>
        Drinks
      </h1>
    </section>
    <!-- Main content -->
    <section class="content">
      <p>
        <a href="drinks/add" class="btn btn-primary">Add Drink</a>
      </p>
      <!-- Default box -->
      <div class="box">
        <div class="box-header with-border">
          <h3 class="box-title">List of Drinks</h3>
        </div>
        <div class="box-body">
		          <table class="table table-bordered">
                <tbody>
                <tr>
                  <th style="width: 10px">#</th>
                  <th>Title</th>
                  <th>Price</th>
                  <th>Time(mins)</th>
                  <th>Action</th>
                </tr>
                {% for drink in drinks %}
                <tr>
                  <td>{{ drink.id }}</td>
                  <td>{{ drink.name }}</td>
                  <td>{{ drink.price }}</td>
                  <td>{{ drink.time }}</td>
                  <td>
                    <a href="/drinks/edit/{{ drink.id }}" class="btn btn-primary">Edit</a>
                    <a href="/drinks/delete/{{ drink.id }}" class="btn btn-danger">Delete</a>
                  </td>
                </tr>
        		{% endfor %}
              </tbody>
          </table>
        </div>
        <div class="box-footer">
          Footer
        </div>
      </div>

    </section>

  </div>

{% endblock %}